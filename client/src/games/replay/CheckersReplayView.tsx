import StubReplayView from "./StubReplayView.tsx";
import type { JSX } from "react";

/** Placeholder until the live Checkers game ships. */
export default function CheckersReplayView(): JSX.Element {
  return <StubReplayView gameLabel="Checkers" />;
}
