import StubReplayView from "./StubReplayView.tsx";
import type { JSX } from "react";

/** Placeholder until the live Connect 4 game ships. */
export default function Connect4ReplayView(): JSX.Element {
  return <StubReplayView gameLabel="Connect 4" />;
}
