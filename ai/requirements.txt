# GameNite Arena training kit — PINNED to the proven stack.
# Real PPO learning on the v2 nim observation was verified end to end on
# exactly these versions; loose ranges caused silent drift between what
# users trained on and what the platform serves. Bump deliberately and
# rerun ai/tests/ (including the slow learning check) before shipping.

# ML stack (training + artifact serialization)
torch==2.11.0
stable-baselines3==2.8.0
gymnasium==1.2.3
numpy==2.2.3

# Inference service (repo-side; harmless in the kit)
fastapi==0.131.0
uvicorn[standard]==0.41.0
pydantic==2.12.5

# Tests (fastapi TestClient needs httpx)
pytest==9.0.2
httpx==0.28.1
