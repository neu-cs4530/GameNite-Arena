[pytest]
testpaths = ai/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: real-training verification bursts (set GAMENITE_SLOW=1 to enable)
